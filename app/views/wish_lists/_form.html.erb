<%= simple_form_for(@wish_list) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :property %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
