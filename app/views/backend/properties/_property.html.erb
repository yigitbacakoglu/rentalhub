<tr>
  <td><%= property.title %></td>
  <td><%= property.city %></td>
  <td><%= property.country %></td>
  <td><%= property.wish_lists.count %></td>
  <td><%= link_to 'Edit', edit_backend_property_path(property) %>
    <%= link_to 'Delete', backend_property_path(property), data: {method: :delete, confirm: 'Are you sure?'} %></td>
</tr>
